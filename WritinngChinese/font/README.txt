WenKai Webfont Setup

Place the following files into this folder:
  WenKai-Regular.woff2
  WenKai-Regular.woff

The HTML is already wired to load from ./font/ and will swap to WenKai when loaded.
If you use a service worker, add these files to your cache list and bump CACHE_NAME.